## Summary

What does this PR do and why.

## Checklist

- [ ] Tests pass locally (`uv run pytest`)
- [ ] Lint passes (`uv run ruff check src tests`)
- [ ] Every number in the report or docs comes from a script
- [ ] No hardcoded symbols (universe comes from config)
- [ ] Data artifacts in data/processed/ have a manifest entry
- [ ] Calibration results are reported as ranges with standard errors

## Reviewers

Tag your sprint reviewers here.
