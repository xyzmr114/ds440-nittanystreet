[package]
name = "taintbox-desktop"
version = "0.1.0"
description = "TaintBox Desktop Application (Tauri v2 Shell)"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
